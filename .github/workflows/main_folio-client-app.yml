name: Build and deploy React app to Azure Web App - folio-client-app

on:
  push:
    branches: 
      - main
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'

      - name: Install dependencies and build
        run: |
          cd client
          npm install
          npm run build

      - name: Zip artifact for deployment
        run: |
          zip -r release.zip client/build

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: client-app
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: client-app

      - name: Unzip artifact
        run: unzip release.zip -d temp

      - name: Move files to wwwroot
        run: |
          mkdir -p wwwroot
          mv temp/client/build/* wwwroot/

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/static-webapps-deploy@v1
        with:
          app-name: 'folio-staticclient-app'
          slot-name: 'Production'
          package: ./wwwroot
